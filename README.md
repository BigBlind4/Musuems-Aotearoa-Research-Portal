How to check Google Sign-in Authentication:

You will need to have Python installed. From the command line cd to the webMock folder and enter:

	python -m SimpleHTTPServer

In chrome, open the webpage:

	localhost:8000